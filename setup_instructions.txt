# 1. To use GUI packages with Linux, you will need to install the following extended dependencies for Qt:
sudo apt-get install libgl1-mesa-glx libegl1-mesa libxrandr2 libxrandr2 libxss1 libxcursor1 libxcomposite1 libasound2 libxi6 libxtst6

# 2. make temporary dir and download anaconda package
cd ~
mkdir tmp
cd tmp
wget https://repo.anaconda.com/archive/Anaconda3-2019.10-Linux-x86_64.sh

# 3. check the hash against https://docs.anaconda.com/anaconda/install/hashes/Anaconda3-2019.10-Linux-x86_64.sh-hash/
sha256sum https://repo.anaconda.com/archive/Anaconda3-2019.10-Linux-x86_64.sh

# 4. install package
bash https://repo.anaconda.com/archive/Anaconda3-2019.10-Linux-x86_64.sh

# 5. press 'enter' key several times for agreement 
#    type 'yes' for default install location
#    type 'yes' to initialize Anaconda3 when installer complete 

#5. reset terminal window for installation to complete
source ~/.bashrc

#6. verify anaconda install
conda list

#7. verify python install
python
quit()

#8. verify anaconda-navigator install
anaconda-navigator
^C

#9. download jet-brains toolbox tarball to tmp dir 
wget https://download.jetbrains.com/toolbox/jetbrains-toolbox-1.16.6067.tar.gz

#10. extract tarball to /opt dir
sudo tar -xzf jetbrains-toolbox-1.16.6067.tar.gz -C /opt

#11. execute the jetbrains-toolbox binary and select to install PyCharm Professional 2019.3 from the popup
cd /opt/jetbrains-toolbox-1.16.6067
./jetbrains-toolbox

#12. login to jetbrains-toolbox with jb username and pw and approve
#    open PyCharm from the toolbox and activate with jb username and pw

#9. download git and make
sudo apt update
sudo apt install git
sudo apt install make
sudo apt install gcc
sudo apt install g++

#10. setup git
git config --global user.name "<name>"
git config --global user.email "<email>"

#11. open PyCharm and setup IDE, login to GitHub
#	 select get from version control
#	 select ai-chess-agent and clone

#12. 
conda create --name ai-chess-agent python=3.6
conda install -n ai-chess-agent numpy pandas matplotlib

#conda install -n ai-chess-agent pandoc pandocfilters nbconvert nbformat jupyter_core numpy pandas matplotlib


in pycharm configure python interpereter, select Conda Environment, existing environment

Interpereter:						/home/<your-username>/anaconda3/envs/ai-chess-agent/bin/python
Conda Executable:					/home/<your-username>/anaconda3/bin/conda
Make Available to all Projects:		Yes


cd ~/PycharmProjects/ai-chess-agent

conda activate ai-chess-agent
pip install python-chess


#13. clone stockfish repo
git clone https://github.com/official-stockfish/Stockfish.git

#14. make the stockfish executable
cd Stockfish/src
make build ARCH=x86-64 COMP=gcc

#15. make new engine dir and copy the stockfish executable
mkdir -p ~/PycharmProjects/ai-chess-agent/src/stockfish_engine/test && cp stockfish "$_"





